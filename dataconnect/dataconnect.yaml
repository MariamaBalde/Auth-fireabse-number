specVersion: "v1beta"
serviceId: "auth-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "auth-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
